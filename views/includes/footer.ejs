<footer class="custom-footer mt-5 py-5">
  <div class="container text-center">
    <div class="f-info d-flex flex-column align-items-center gap-3">

      <!-- Social Icons -->
      <div class="f-info-socials d-flex gap-4 justify-content-center">
        <a href="#" class="social-icon" aria-label="Instagram"><i class='bx bxl-instagram'></i></a>
        <a href="#" class="social-icon" aria-label="Meta"><i class='bx bxl-meta'></i></a>
        <a href="#" class="social-icon" aria-label="LinkedIn"><i class='bx bxl-linkedin'></i></a>
      </div>

      <!-- Brand -->
      <div class="text-muted small opacity-75">© 2025 TravelGo Private Limited</div>

      <!-- Footer Links -->
      <div class="f-info-links d-flex gap-4 justify-content-center">
        <a href="/privacy" class="footer-link">Privacy</a>
        <a href="/terms" class="footer-link">Terms</a>
      </div>

    </div>
  </div>
</footer>

<style>
  /* Footer base style matching navbar with shadow and rounded corners */
  .custom-footer {
    background-color: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(14px);
    border-top: 2px solid #eee;
    box-shadow: 0 4px 20px rgba(0,0,0,0.07);
    border-radius: 12px 12px 0 0;
    color: #444;
    animation: fadeInUp 0.8s ease forwards;
    opacity: 0;
    transform: translateY(15px);
  }

  /* Fade-in and slide-up animation */
  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Social Icons */
  .social-icon {
    color: #888;
    font-size: 1.9rem;
    transition: color 0.35s ease, transform 0.35s ease, filter 0.35s ease;
    filter: drop-shadow(0 0 0 rgba(0,0,0,0)); /* start with no shadow */
  }

  .social-icon:hover {
    color: #ff385c;
    transform: translateY(-6px) scale(1.15);
    filter: drop-shadow(0 4px 6px rgba(255, 56, 92, 0.4));
  }

  /* Footer Links */
  .footer-link {
    color: #666;
    font-weight: 600;
    text-decoration: none;
    font-size: 0.9rem;
    transition: color 0.35s ease, transform 0.3s ease;
  }

  .footer-link:hover {
    color: #ff385c;
    transform: translateY(-2px);
    text-decoration: underline;
  }

  /* Brand text */
  .custom-footer .text-muted {
    color: #777 !important;
    font-size: 0.85rem;
  }

  /* Responsive tweaks */
  @media (max-width: 576px) {
    .f-info-links {
      flex-direction: column;
      gap: 1rem;
    }
  }
</style>
