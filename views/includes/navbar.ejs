<!-- Navbar -->
<nav class="navbar navbar-expand-md navbar-light bg-white shadow-sm border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand d-flex align-items-center gap-2" href="/">
      <i class='bx bx-compass bx-md' style="color: crimson;"></i>
      <span class="fw-bold text-dark">TravelGo</span>
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup"
      aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
      <div class="navbar-nav align-items-center">
        <a class="nav-link text-dark fw-semibold px-3" href="/">Home</a>
        <a class="nav-link text-dark fw-semibold px-3" href="/listings">All Listings</a>
        <a class="nav-link text-dark fw-semibold px-3" href="/listings/new">New Listing</a>

        <!-- Auth Buttons -->
        <div class="d-flex ms-3">
          <% if (!currentUser) { %>
            <!-- Show Login & Signup if not logged in -->
            <a href="/login" class="btn btn-outline-primary fw-semibold me-2">
              <i class='bx bx-log-in me-1'></i> Login
            </a>
            <a href="/signup" class="btn btn-primary fw-semibold">
              <i class='bx bx-user-plus me-1'></i> Sign Up
            </a>
          <% } else { %>
            <!-- Show Logout if logged in -->
            <form action="/logout" method="POST" class="d-inline">
              <button type="submit" class="btn btn-danger fw-semibold">
                <i class='bx bx-log-out me-1'></i> Logout
              </button>
            </form>
          <% } %>
        </div>
      </div>
    </div>
  </div>
</nav>

<!-- Bootstrap JS Bundle -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
